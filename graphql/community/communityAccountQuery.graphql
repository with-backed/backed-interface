query communityAccount($address: ID!) {
  account(id: $address) {
    id
    token {
      id
      uri
    }
    categoryScoreChanges {
      timestamp
      category {
        name
      }
      ipfsEntryHash
    }
  }
}
