query allLoans(
  $where: Loan_filter
  $first: Int
  $orderBy: Loan_orderBy
  $orderDirection: OrderDirection
) {
  loans(
    where: $where
    orderBy: $orderBy
    orderDirection: $orderDirection
    first: $first
  ) {
    ...allLoanProperties
  }
}
